---
const { href, class:extraClasses, active } = Astro.props;  
---

<a 
  href={href} 
  class={`
    bg-blue-500 text-white px-4 py-2 rounded-lg
    ${extraClasses}
    ${active && 'border-2 border-red-500'}
  `}
>
<slot name="before-text"/>
  <slot />
<slot name="after-text"/>

</a>